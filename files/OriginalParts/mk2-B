PART
{
	name = commandPod_1p5
	module = Part
	author = steedcrugeon, Snark
	MODEL
	{
		// TODO: verify name of model file
		model = MissingHistory/assets/commandPod_1p5
	}
	scale = 1
	rescaleFactor = 1
	// TODO: Adjust node positions to match model. These values are an
	// approximate guess.
	node_stack_top = 0.0, 0.7, 0.0, 0.0, 1.0, 0.0, 1
	node_stack_bottom = 0.0, -0.5, 0.0, 0.0, -1.0, 0.0, 2
	CoMOffset = 0.0, -0.2, 0.0
	CoPOffset = 0.0, 0.2, 0.0
	CrewCapacity = 2
	TechRequired = advFlightControl
	entryCost = 4000
	cost = 2800
	category = Pods
	subcategory = 0
	title = Mk2-B Command Pod
	manufacturer = #autoLOC_501662 // C7 Aerospace Division
	description = Slightly squatter and wider than the Mk2 pod, this capsule is suitable for either flight operations or as a lander.
	attachRules = 1,0,1,1,0
	mass = 1.56
	dragModelType = default
	maximum_drag = 0.2
	minimum_drag = 0.3
	angularDrag = 2
	crashTolerance = 14
	breakingForce = 150
	breakingTorque = 150
	maxTemp = 2200 // = 3000
	vesselType = Ship

	bulkheadProfiles = size1p5
	tags = #autoLOC_8310156  // same tags as Mk2 pod
	INTERNAL
	{
		// TODO: update to point at appropriate IVA
		name = commandPod1p5Internals
	}

	MODULE
	{
		name = ModuleColorChanger
		shaderProperty = _EmissiveColor
		animRate = 0.8
		animState = false
		useRate = true
		toggleInEditor = true
		toggleInFlight = true
		toggleInFlight = true
		unfocusedRange = 5
		toggleName = #autoLOC_502011 // #autoLOC_502011 = Toggle Lights
		eventOnName = #autoLOC_502012 // #autoLOC_502012 = Lights On
		eventOffName = #autoLOC_502013 // #autoLOC_502013 = Lights Off
		toggleAction = True
		defaultActionGroup = Light
		redCurve
		{
			key = 0 0 0 3
			key = 1 1 0 0
		}
		greenCurve
		{
			key = 0 0 0 1
			key = 1 1 1 0
		}
		blueCurve
		{
			key = 0 0 0 0
			key = 1 0.7 1.5 0
		}
		alphaCurve
		{
			key = 0 1
		}
		// TODO: For each mesh in the model that's a "cabin window" that needs
		// to light up when the lights are toggled in-game, add an includedRenderer
		// entry here. Why this is needed: If you don't have any includedRenderer
		// lines, then by default, ModuleColorChanger will hog *all* the meshes on
		// the part.  That's bad, because it breaks the F11 thermal overlay (the
		// part won't show any highlights). Sure, the Squad stock parts have that
		// bug, but we can be better than that.  :-)  Also, making it properly
		// behaved here makes the part friendlier for adding IndicatorLights meshes
		// to it (without the includedRenderer lines, the ModuleColorChanger ends
		// up stomping all over the indicator lights, too.) For each entry, the
		// name of the includedRenderer is the mesh name in the model.
		includedRenderer = windows
		includedRenderer = porthole
	}

	MODULE
	{
		name = ModuleCommand
		minimumCrew = 1
	}

	RESOURCE
	{
		name = ElectricCharge
		amount = 200
		maxAmount = 200
	}

	RESOURCE
	{
		name = MonoPropellant
		amount = 20
		maxAmount = 20
	}

	MODULE
	{
		name = ModuleReactionWheel
		PitchTorque = 9
		YawTorque = 9
		RollTorque = 9
		RESOURCE
		{
			name = ElectricCharge
			rate = 0.45
		}
	}

	MODULE
	{
		name = ModuleScienceExperiment
		experimentID = crewReport
		experimentActionName = #autoLOC_502009 // Crew Report
		resetActionName = #autoLOC_502010 // Discard Crew Report
		reviewActionName = #autoLOC_502200 // Review Report
		useStaging = False
		useActionGroups = True
		hideUIwhenUnavailable = True
		rerunnable = True
		xmitDataScalar = 1.0
		usageReqMaskInternal = 5
		usageReqMaskExternal = -1
	}

	MODULE
	{
		name = ModuleScienceContainer
		reviewActionName = Review Stored Data
		storeActionName = Store Experiments
		evaOnlyStorage = True
		storageRange = 1.8
	}

	MODULE
	{
		name = ModuleDataTransmitter
		antennaType = INTERNAL
		packetInterval = 1.0
		packetSize = 2
		packetResourceCost = 12.0
		requiredResource = ElectricCharge
		antennaPower = 5000
		optimumRange = 2500
		packetFloor = .1
		packetCeiling = 5
	}

	MODULE
	{
		name = FlagDecal
		// TODO: make sure this matches the model
		textureQuadName = flagTransform
	}
}
